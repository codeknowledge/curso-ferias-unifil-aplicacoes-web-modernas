interface JQuery {
    characterCounter();
    leanModal();
    leanModal(configuration: Object);
    closeModal();
    dropdown(configuration: Object);
}

declare class Materialize {
    static showStaggeredList(selector: string);
}